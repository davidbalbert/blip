There are two different possibly independent axes to borrowing when we think about the consuming when we think about about resource cleanup. up. There's the first is pointers. So obviously a pointer is a borrow, and you can borrow uniquely or you can borrow shared. but there's another type let's say we're thinking about open, close, read-in, right? the unix system calls and the the ... what those APIs would be idiomatic translation to blip. Open returns a file descriptor and a file descriptor has to be well it's not that the file descriptor has to be unique necessarily but the file descriptor is resource owning which means it's linear. It has to be, um, cleaned up, so it's required to be used once and read and write should borrow. and quote is should consume. So it's required to be consumed. one way to do the API would be to have the borrows take pointers but it doesn't actually have to be a pointer right it just has to be a borrow it's an int and so So, you know, like in Swift, where you have consuming and borrowing, you know, which don't have to be pointers, the data can actually just be copied. and if we want to mirror the the Unix API that keep things but add the sort of enforced cleanup, up you'd want read and write to take a a take the file descriptor directly, but not, but not consume it. and so that's the other axes, right? You can, from a resource management perspective, You can have a borrowing thing that doesn't necessarily take a pointer.

How do we handle this? can we make a non-escape of a non-escaping linear type Fiat Barra. I'm not sure I need to think.
